<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌟 Brasil Verso - Countryballs 🚀</title>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&family=Nunito:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --space-purple: #6B4BC4;
            --space-blue: #2E5BD9;
            --space-pink: #FF6B9D;
            --space-yellow: #FFD93D;
            --space-orange: #FF8C42;
            --space-cyan: #4ECDC4;
            --dark-space: #1A0F2E;
            --light-purple: #B8A8E6;
        }

        body {
            font-family: 'Nunito', sans-serif;
            background: linear-gradient(135deg, #1A0F2E 0%, #2E1B52 50%, #1A0F2E 100%);
            color: white;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle 3s infinite ease-in-out;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.3); }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        .header {
            text-align: center;
            padding: 40px 20px;
            animation: float 6s infinite ease-in-out;
        }

        .channel-logo {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            border: 6px solid var(--space-yellow);
            box-shadow: 0 0 30px rgba(255, 217, 61, 0.6),
                        0 0 60px rgba(255, 107, 157, 0.4);
            margin-bottom: 20px;
            animation: pulse 2s infinite ease-in-out;
            object-fit: cover;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .header h1 {
            font-family: 'Fredoka', sans-serif;
            font-size: 3.5em;
            font-weight: 700;
            background: linear-gradient(45deg, var(--space-yellow), var(--space-pink), var(--space-cyan));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
            text-shadow: 0 0 20px rgba(255, 217, 61, 0.5);
        }

        .header p {
            font-size: 1.3em;
            color: var(--light-purple);
            font-weight: 600;
        }

        .stats-bar {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 30px;
            margin: 20px 0;
            flex-wrap: wrap;
        }

        .stat {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 15px 30px;
            border-radius: 20px;
            border: 2px solid var(--space-cyan);
            box-shadow: 0 5px 15px rgba(78, 205, 196, 0.4);
        }

        .stat-number {
            font-size: 2em;
            font-weight: 900;
            color: var(--space-yellow);
            font-family: 'Fredoka', sans-serif;
        }

        .stat-label {
            font-size: 0.9em;
            color: var(--light-purple);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .tabs {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin: 40px 0;
            flex-wrap: wrap;
        }

        .tab {
            background: linear-gradient(135deg, var(--space-purple), var(--space-blue));
            border: none;
            padding: 15px 35px;
            border-radius: 50px;
            color: white;
            font-family: 'Fredoka', sans-serif;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(107, 75, 196, 0.4);
            position: relative;
            overflow: hidden;
        }

        .tab:before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .tab:hover:before {
            width: 300px;
            height: 300px;
        }

        .tab:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 10px 25px rgba(107, 75, 196, 0.6);
        }

        .tab.active {
            background: linear-gradient(135deg, var(--space-pink), var(--space-orange));
            box-shadow: 0 8px 20px rgba(255, 107, 157, 0.6);
            transform: scale(1.1);
        }

        .tab-content {
            display: none;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 30px;
            padding: 40px;
            margin: 30px 0;
            border: 3px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            animation: fadeIn 0.5s ease-in-out;
        }

        .tab-content.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .tab-content h2 {
            font-family: 'Fredoka', sans-serif;
            font-size: 2.5em;
            color: var(--space-yellow);
            margin-bottom: 20px;
            text-align: center;
        }

        .tab-content p {
            font-size: 1.2em;
            line-height: 1.8;
            margin-bottom: 15px;
        }

        .video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }

        .video-card {
            background: linear-gradient(135deg, rgba(107, 75, 196, 0.3), rgba(46, 91, 217, 0.3));
            border-radius: 20px;
            overflow: hidden;
            border: 2px solid rgba(255, 217, 61, 0.4);
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .video-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(255, 217, 61, 0.4);
            border-color: var(--space-yellow);
        }

        .video-thumbnail {
            width: 100%;
            height: 180px;
            object-fit: cover;
        }

        .video-info {
            padding: 15px;
        }

        .video-card h3 {
            color: var(--space-cyan);
            font-family: 'Fredoka', sans-serif;
            margin-bottom: 8px;
            font-size: 1.1em;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .video-card p {
            font-size: 0.9em;
            color: var(--light-purple);
            margin: 0;
        }

        .loading {
            text-align: center;
            padding: 40px;
            font-size: 1.5em;
            color: var(--space-cyan);
        }

        .milestone {
            background: linear-gradient(135deg, rgba(107, 75, 196, 0.2), rgba(46, 91, 217, 0.2));
            border-radius: 20px;
            padding: 25px;
            margin: 20px 0;
            border: 2px solid rgba(255, 217, 61, 0.3);
        }

        .milestone-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            flex-wrap: wrap;
            gap: 10px;
        }

        .milestone-title {
            font-family: 'Fredoka', sans-serif;
            font-size: 1.4em;
            color: var(--space-yellow);
        }

        .milestone-count {
            font-family: 'Fredoka', sans-serif;
            font-size: 1.2em;
            color: var(--space-cyan);
        }

        .progress-bar {
            background: rgba(255, 255, 255, 0.1);
            height: 30px;
            border-radius: 15px;
            overflow: hidden;
            position: relative;
            border: 2px solid rgba(255, 255, 255, 0.2);
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--space-pink), var(--space-orange), var(--space-yellow));
            border-radius: 15px;
            transition: width 1s ease-out;
            position: relative;
            overflow: hidden;
        }

        .progress-fill::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            animation: shimmer 2s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .milestone.completed .progress-fill {
            background: linear-gradient(90deg, var(--space-cyan), var(--space-blue), var(--space-purple));
        }

        .milestone-emoji {
            font-size: 2em;
            margin-right: 10px;
        }

        .flag-section {
            text-align: center;
            margin: 40px 0;
        }

        .countryball-flag {
            width: 300px;
            height: 300px;
            border-radius: 20px;
            border: 5px solid var(--space-cyan);
            box-shadow: 0 0 40px rgba(78, 205, 196, 0.6);
            transition: all 0.3s ease;
            object-fit: cover;
        }

        .countryball-flag:hover {
            transform: scale(1.1) rotate(5deg);
            box-shadow: 0 0 60px rgba(78, 205, 196, 0.8);
        }

        .social-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .social-btn {
            background: linear-gradient(135deg, var(--space-pink), var(--space-orange));
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-family: 'Fredoka', sans-serif;
            font-weight: 600;
            font-size: 1.1em;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 107, 157, 0.4);
            display: inline-block;
        }

        .social-btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 10px 25px rgba(255, 107, 157, 0.6);
        }

        .planet {
            position: fixed;
            border-radius: 50%;
            animation: orbit 20s infinite linear;
            pointer-events: none;
            z-index: 0;
        }

        .planet1 {
            width: 100px;
            height: 100px;
            background: radial-gradient(circle at 30% 30%, var(--space-pink), var(--space-purple));
            top: 10%;
            right: 5%;
            box-shadow: 0 0 40px var(--space-pink);
        }

        .planet2 {
            width: 150px;
            height: 150px;
            background: radial-gradient(circle at 30% 30%, var(--space-cyan), var(--space-blue));
            bottom: 15%;
            left: 8%;
            animation-duration: 30s;
            box-shadow: 0 0 50px var(--space-cyan);
        }

        @keyframes orbit {
            0% { transform: rotate(0deg) translateX(30px) rotate(0deg); }
            100% { transform: rotate(360deg) translateX(30px) rotate(-360deg); }
        }

        .api-key-notice {
            background: linear-gradient(135deg, rgba(255, 107, 157, 0.3), rgba(255, 140, 66, 0.3));
            border: 2px solid var(--space-orange);
            border-radius: 15px;
            padding: 20px;
            margin: 20px 0;
            text-align: center;
        }

        .api-key-notice h3 {
            color: var(--space-yellow);
            margin-bottom: 10px;
        }

        .api-key-notice a {
            color: var(--space-cyan);
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 2.5em;
            }
            
            .tab {
                padding: 12px 25px;
                font-size: 1em;
            }
            
            .tab-content {
                padding: 25px;
            }
            
            .countryball-flag {
                width: 200px;
                height: 200px;
            }

            .video-grid {
                grid-template-columns: 1fr;
            }

            .stats-bar {
                gap: 15px;
            }

            .stat {
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="stars" id="stars"></div>
    <div class="planet planet1"></div>
    <div class="planet planet2"></div>

    <div class="container">
        <div class="header">
            <img src="/mnt/user-data/uploads/BrasilVersoPhoto.png" alt="Brasil Verso Canal" class="channel-logo">
            <h1>🚀 Brasil Verso 🌟</h1>
            <p>Countryballs espaciais com edits incríveis!</p>
            
            <div class="stats-bar">
                <div class="stat">
                    <div class="stat-number" id="subscriber-count">...</div>
                    <div class="stat-label">Inscritos</div>
                </div>
                <div class="stat">
                    <div class="stat-number" id="video-count">...</div>
                    <div class="stat-label">Vídeos</div>
                </div>
                <div class="stat">
                    <div class="stat-number" id="view-count">...</div>
                    <div class="stat-label">Visualizações</div>
                </div>
            </div>
        </div>

        <div class="tabs">
            <button class="tab active" onclick="openTab(event, 'sobre')">🌍 Sobre</button>
            <button class="tab" onclick="openTab(event, 'marcos')">🏆 Marcos</button>
            <button class="tab" onclick="openTab(event, 'videos')">🎬 Vídeos</button>
            <button class="tab" onclick="openTab(event, 'bandeira')">🎨 Nossa Bandeira</button>
            <button class="tab" onclick="openTab(event, 'contato')">💌 Contato</button>
        </div>

        <div id="sobre" class="tab-content active">
            <h2>✨ Bem-vindo ao Brasil Verso! ✨</h2>
            <p>🌟 Somos um canal dedicado aos Countryballs! Começamos criando histórias envolventes e divertidas, e hoje evoluímos para criar edits incríveis que exploram o universo dos countryballs de forma única!</p>
            <p>🚀 Nossa missão é combinar criatividade, humor brasileiro e muito amor pelos countryballs para entregar conteúdo de qualidade!</p>
            <p>🎨 Com quase 150 vídeos no canal, estamos sempre trazendo novidades e edits que você vai amar! Faça parte da nossa comunidade espacial!</p>
        </div>

        <div id="marcos" class="tab-content">
            <h2>🏆 Marcos do Canal</h2>
            <p style="text-align: center; margin-bottom: 30px;">Acompanhe nossa jornada rumo às estrelas! 🌟</p>
            
            <div id="milestones-container">
                <!-- Milestones serão inseridos aqui dinamicamente -->
            </div>
        </div>

        <div id="videos" class="tab-content">
            <h2>🎬 Nossos Vídeos</h2>
            
            <div class="api-key-notice" id="api-notice">
                <h3>⚠️ Configure sua API Key do YouTube</h3>
                <p>Para exibir os vídeos automaticamente, você precisa adicionar sua chave da API do YouTube.</p>
                <p>Obtenha gratuitamente em: <a href="https://console.cloud.google.com/apis/credentials" target="_blank">Google Cloud Console</a></p>
                <p style="margin-top: 10px; font-size: 0.9em;">Edite o código HTML e adicione sua chave na variável <code>YOUTUBE_API_KEY</code></p>
            </div>
            
            <div class="loading" id="videos-loading">Carregando vídeos...</div>
            <div class="video-grid" id="video-grid"></div>
        </div>

        <div id="bandeira" class="tab-content">
            <h2>🎨 Nossa Bandeira Countryball</h2>
            <div class="flag-section">
                <img src="/mnt/user-data/uploads/BrasilVersoOFC.png" alt="Bandeira Brasil Verso" class="countryball-flag">
                <p style="margin-top: 20px; font-size: 1.3em;">🌟 A bandeira oficial do Brasil Verso! 🌟</p>
                <p>Um símbolo espacial que representa nossa paixão pelos countryballs e nossas raízes brasileiras!</p>
            </div>
        </div>

        <div id="contato" class="tab-content">
            <h2>💌 Entre em Contato!</h2>
            <p style="text-align: center;">🌟 Quer fazer parte da nossa comunidade espacial? 🌟</p>
            <p style="text-align: center;">Inscreva-se no canal e ative o sininho para não perder nenhum vídeo novo!</p>
            <div class="social-buttons">
                <a href="https://www.youtube.com/@brasilversocountryballs" target="_blank" class="social-btn">📺 YouTube</a>
            </div>
        </div>
    </div>

    <script>
        // ⚠️ IMPORTANTE: Adicione sua chave da API do YouTube aqui
        // Obtenha gratuitamente em: https://console.cloud.google.com/apis/credentials
        const YOUTUBE_API_KEY = 'AIzaSyArjH7O7K-azAfex2crVFETyEKco9LG50g';
        const CHANNEL_ID = 'UClL__yI571jZytTeOIEi1uQ';

        // Criar estrelas aleatórias
        function createStars() {
            const starsContainer = document.getElementById('stars');
            const numberOfStars = 100;

            for (let i = 0; i < numberOfStars; i++) {
                const star = document.createElement('div');
                star.className = 'star';
                star.style.width = Math.random() * 3 + 'px';
                star.style.height = star.style.width;
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.animationDelay = Math.random() * 3 + 's';
                starsContainer.appendChild(star);
            }
        }

        // Função para trocar abas
        function openTab(evt, tabName) {
            const tabContents = document.getElementsByClassName('tab-content');
            for (let i = 0; i < tabContents.length; i++) {
                tabContents[i].classList.remove('active');
            }

            const tabs = document.getElementsByClassName('tab');
            for (let i = 0; i < tabs.length; i++) {
                tabs[i].classList.remove('active');
            }

            document.getElementById(tabName).classList.add('active');
            evt.currentTarget.classList.add('active');
        }

        // Formatar número com separadores
        function formatNumber(num) {
            if (num >= 1000000) {
                return (num / 1000000).toFixed(1) + 'M';
            } else if (num >= 1000) {
                return (num / 1000).toFixed(1) + 'K';
            }
            return num.toString();
        }

        // Criar marcos de inscritos
        function createMilestones(subscriberCount) {
            const milestones = [
                { emoji: '🎯', target: 100, label: '100 Inscritos' },
                { emoji: '🎪', target: 500, label: '500 Inscritos' },
                { emoji: '🎨', target: 1000, label: '1K Inscritos' },
                { emoji: '🎭', target: 2500, label: '2.5K Inscritos' },
                { emoji: '🎬', target: 5000, label: '5K Inscritos' },
                { emoji: '🎸', target: 10000, label: '10K Inscritos' },
                { emoji: '🎤', target: 25000, label: '25K Inscritos' },
                { emoji: '🏆', target: 50000, label: '50K Inscritos' },
                { emoji: '👑', target: 100000, label: '100K Inscritos' },
                { emoji: '💎', target: 250000, label: '250K Inscritos' },
                { emoji: '🌟', target: 500000, label: '500K Inscritos' },
                { emoji: '🚀', target: 1000000, label: '1M Inscritos' }
            ];

            const container = document.getElementById('milestones-container');
            container.innerHTML = '';

            milestones.forEach(milestone => {
                const progress = Math.min((subscriberCount / milestone.target) * 100, 100);
                const isCompleted = subscriberCount >= milestone.target;
                
                const milestoneDiv = document.createElement('div');
                milestoneDiv.className = 'milestone' + (isCompleted ? ' completed' : '');
                
                milestoneDiv.innerHTML = `
                    <div class="milestone-header">
                        <div class="milestone-title">
                            <span class="milestone-emoji">${milestone.emoji}</span>
                            ${milestone.label}
                        </div>
                        <div class="milestone-count">${formatNumber(subscriberCount)} / ${formatNumber(milestone.target)}</div>
                    </div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: ${progress}%"></div>
                    </div>
                `;
                
                container.appendChild(milestoneDiv);
            });
        }

        // Buscar estatísticas do canal
        async function fetchChannelStats() {
            if (YOUTUBE_API_KEY === 'SUA_API_KEY_AQUI') {
                document.getElementById('subscriber-count').textContent = '???';
                document.getElementById('video-count').textContent = '???';
                document.getElementById('view-count').textContent = '???';
                createMilestones(0);
                return null;
            }

            try {
                const response = await fetch(
                    `https://www.googleapis.com/youtube/v3/channels?part=statistics,snippet&id=${CHANNEL_ID}&key=${YOUTUBE_API_KEY}`
                );
                
                if (!response.ok) throw new Error('Erro ao buscar dados');
                
                const data = await response.json();
                if (data.items && data.items.length > 0) {
                    const stats = data.items[0].statistics;
                    const subscriberCount = parseInt(stats.subscriberCount);
                    
                    document.getElementById('subscriber-count').textContent = formatNumber(subscriberCount);
                    document.getElementById('video-count').textContent = formatNumber(parseInt(stats.videoCount));
                    document.getElementById('view-count').textContent = formatNumber(parseInt(stats.viewCount));
                    
                    createMilestones(subscriberCount);
                    
                    return data.items[0];
                }
            } catch (error) {
                console.error('Erro ao buscar estatísticas:', error);
                document.getElementById('subscriber-count').textContent = 'Erro';
                document.getElementById('video-count').textContent = 'Erro';
                document.getElementById('view-count').textContent = 'Erro';
            }
            return null;
        }

        // Buscar vídeos do canal
        async function fetchChannelVideos() {
            if (YOUTUBE_API_KEY === 'SUA_API_KEY_AQUI') {
                document.getElementById('videos-loading').style.display = 'block';
                document.getElementById('video-grid').innerHTML = '';
                return;
            }

            try {
                document.getElementById('api-notice').style.display = 'none';
                
                let allVideos = [];
                let nextPageToken = '';
                
                // Buscar todos os vídeos (paginação)
                do {
                    const response = await fetch(
                        `https://www.googleapis.com/youtube/v3/search?part=snippet&channelId=${CHANNEL_ID}&maxResults=50&order=date&type=video&pageToken=${nextPageToken}&key=${YOUTUBE_API_KEY}`
                    );
                    
                    if (!response.ok) throw new Error('Erro ao buscar vídeos');
                    
                    const data = await response.json();
                    allVideos = allVideos.concat(data.items);
                    nextPageToken = data.nextPageToken || '';
                    
                } while (nextPageToken);
                
                document.getElementById('videos-loading').style.display = 'none';
                
                const videoGrid = document.getElementById('video-grid');
                videoGrid.innerHTML = '';
                
                allVideos.forEach(video => {
                    const videoCard = document.createElement('div');
                    videoCard.className = 'video-card';
                    videoCard.onclick = () => window.open(`https://www.youtube.com/watch?v=${video.id.videoId}`, '_blank');
                    
                    videoCard.innerHTML = `
                        <img src="${video.snippet.thumbnails.medium.url}" alt="${video.snippet.title}" class="video-thumbnail">
                        <div class="video-info">
                            <h3>${video.snippet.title}</h3>
                            <p>${new Date(video.snippet.publishedAt).toLocaleDateString('pt-BR')}</p>
                        </div>
                    `;
                    
                    videoGrid.appendChild(videoCard);
                });
                
            } catch (error) {
                console.error('Erro ao buscar vídeos:', error);
                document.getElementById('videos-loading').innerHTML = '❌ Erro ao carregar vídeos. Verifique sua API Key.';
            }
        }

        // Inicializar ao carregar a página
        window.onload = async function() {
            createStars();
            await fetchChannelStats();
            await fetchChannelVideos();
        };
    </script>
</body>
</html>